[project]
name = "identity-probe"
version = "0.1.0"
description = "Probes explaining why cell-line gene representations hurt cold-cell-line generalization"
requires-python = ">=3.10"
dependencies = [
    "pandas",
    "numpy",
    "scikit-learn",
    "scipy",
    "matplotlib",
    "torch",
]
